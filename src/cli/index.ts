// Here the cli for the machine learning will be placed
// interactive live programm
// waiting for messages, then returning results, all as json strings

// initGame: [bool,bool,bool,bool] => {gs}
// nextGSs: {currentGS} => [{gs},...]
// evalGS: {{gs},depth} => [number,number,number,number]
