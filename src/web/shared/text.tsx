import React, { FC } from 'react';

// -----------------------------------------------------------------------------

const component: FC = props => {
    return <>{props.children}</>
};
export default component;
