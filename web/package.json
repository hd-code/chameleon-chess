{
  "name": "@chameleon-chess/web",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "all": "run-s format check lint test build",
    "build": "react-scripts build",
    "check": "tsc",
    "format": "prettier --write public src",
    "lint": "eslint src",
    "start": "react-scripts start",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "@chameleon-chess/logic": "file:../logic",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.0.28",
    "@types/react-dom": "^18.0.11",
    "react-scripts": "5.0.1"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  }
}
