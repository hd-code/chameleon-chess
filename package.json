{
  "name": "chameleon-chess",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "format": "prettier --write src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "prettier": "^3.2.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
